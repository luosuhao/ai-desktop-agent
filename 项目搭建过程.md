# AI 桌面端系统 — 项目搭建过程

## 一、项目概述

本实验要求实现一个面向真实工程代码、复杂文档和数据分析任务的 AI 桌面系统，包含三大模块：Coding Agent、RAG 文档系统、Skill 技能系统。

**技术栈选型**：
- 后端：Python FastAPI + Uvicorn
- 前端：React 18 + TypeScript + Ant Design + Vite
- API 接入：OpenAI SDK（兼容 DeepSeek API）
- 文档处理：PyPDF2 / pdfminer / python-docx / python-pptx / openpyxl / pytesseract
- 数据分析：pandas / numpy / matplotlib / seaborn
- 可视化与文档：python-pptx

---

## 二、搭建步骤

### 步骤 1：项目初始化

```
大作业/
├── backend/           # Python FastAPI 后端
├── frontend/          # React + Vite 前端
├── skills/            # 外部 Skill 文件
└── test_data/         # 测试数据
```

创建目录结构，初始化后端 requirements.txt 和前端 package.json。

**后端依赖**：fastapi, uvicorn, pydantic, openai, python-docx, python-pptx, openpyxl, pandas, numpy, PyPDF2, pdfminer, pytesseract, Pillow, matplotlib, seaborn 等 22 个包。

**前端依赖**：react, react-dom, antd, @ant-design/icons, axios, recharts, dayjs, vite, typescript 等 12 个包。

### 步骤 2：后端核心框架搭建

**config.py** — 配置管理
- Settings 类（API 地址、端口、模型参数、目录路径）
- model_config.json 的加载与保存（UTF-8 BOM 兼容）

**main.py** — FastAPI 主应用
- CORS 中间件配置
- 全局实例：ModelAdapter、CodingAgent、OptimizedAgentLoop、CodeGraph、VectorStore、LLMWiki、SkillManager
- 35+ 个 REST API 端点
- 文件上传与下载服务

### 步骤 3：Coding Agent 模块

**agent/__init__.py** — Coding Agent 核心
- ModelAdapter：封装 OpenAI SDK，支持 DeepSeek / OpenAI / 自定义 API
- ToolRegistry：工具注册中心（8 个工具：read_file / write_file / execute_command / create_checkpoint / rollback_checkpoint / list_directory / run_tests / get_git_diff）
- CodingAgent：多轮 agent loop（execute_task），含任务规划→执行→测试→迭代
- Checkpoint 跨任务持久化（self.checkpoints 独立于 task_state）
- 回滚自动删除新建文件（_save_initial_files 初始文件扫描）
- 文件版本追踪（self.file_versions）+ 代码 Diff 生成（difflib.unified_diff）
- 对话会话记录：每个会话独立保存消息/Checkpoint/版本，持久化到 `outputs/conversation_history.json`

> **修复**：DeepSeek API 要求 assistant 消息的 tool_calls 格式严格，content 为空时必须设为 null
> **修复**：Checkpoint 在 task_state 中跨任务丢失（改为实例级 self.checkpoints 持久保存）
> **修复**：回滚后 Checkpoint 被删无法多级回滚（改为仅删除被回滚的 checkpoint）

**agent/reasonix.py** — Reasonix 缓存优化
- CacheMetrics：追踪命中率、延迟、token 节省、成本节省
- OptimizedAgentLoop：稳定前缀 prompt、滚动状态压缩、cache-first agent loop（execute_with_cache）

**agent/codegraph.py** — CodeGraph 代码图谱
- SymbolInfo：符号信息数据结构
- CodeGraph：仓库扫描、符号提取（Python/JS/TS/C++）、调用图构建、导入依赖图、测试关联

### 步骤 4：RAG 文档系统

**rag/document_parser.py** — 多格式文档解析
- ParsedDocument：解析结果数据结构
- DocumentParser：支持 PDF（pdfminer/PyPDF2）、Word（python-docx）、PPT（python-pptx）、Excel（openpyxl）、CSV、图片（pytesseract OCR）、Markdown/TXT
- 自动生成 RAG 分块

**rag/table_recognizer.py** — 表格结构识别
- Cell / TableStructure：表格数据结构（支持合并单元格 rowspan/colspan）
- TableRecognizer：支持 4 种输入格式（文本/Markdown/CSV/HTML） + 图片 OCR
- 输出 Markdown / CSV / JSON + 复杂度分析

> **修复**：CSV 内容被当作文件路径打开（改为逗号替换为 Tab 后文本解析）
> **修复**：data 字段返回扁平单元格（新增 data_rows 二维数组供前端渲染）

**rag/vector_store.py** — 向量检索
- 伪嵌入（基于 hash，无需 sentence-transformers）
- 向量搜索 / BM25 搜索 / 混合搜索 + 重排序
- 索引持久化

> **修复**：sentence_transformers 导入抛出 NameError 而非 ImportError（改为 except Exception）

**rag/llm_wiki.py** — LLM Wiki 知识库
- WikiPage：文档卡片、章节摘要、概念页面、表格说明
- 关键词搜索 + 持久化存储

> **修复**：defaultdict 调用 most_common() 报错（改为 Counter）

### 步骤 5：Skill 技能系统

**skills/skill_manager.py** — Skill 管理器
- Skill 抽象基类（name, description, trigger, input_schema, workflow, tools, templates, validation）
- SkillManager：注册、匹配、执行、历史记录、动态加载

**skills/markdown_report_skill.py** — Markdown 报告生成
- 直接生成 .md 文件，无需外部编译
- 支持标题层级、表格、图片、参考文献
- 报告自动生成 Markdown 格式

**skills/word_skill.py** — Word 实验报告
- 封面页 + 目录 + 格式化章节 + 表格 + 图片

> **修复**：中文文件名保存失败（添加 re.sub 过滤特殊字符）

**skills/doc_ppt_online_skill.py** — doc-ppt 在线（图片风格 PPT）
- 包装外部 codex-ppt SKILL.md，需 OPENAI_API_KEY

**skills/doc_ppt_offline_skill.py** — doc-ppt 离线（原生可编辑 PPT）
- 运行 doc-to-native-ppt2 流水线：抽取→文本层→设计→原生 PPTX

**skills/excel_ppt_skill.py** — excel-ppt（Excel→PPT 内容规划）
- 运行 ppt-template-generator 完整 7 步流水线产出 PPTX

### 步骤 6：前端界面

**frontend/src/App.tsx** — 主布局
- Ant Design 暗色主题
- 侧边栏导航（8 个菜单项）
- 顶部标题栏

**8 个功能页面**：

| 页面 | 功能 |
|------|------|
| AgentPage | Coding Agent 执行、Cache 指标、CodeGraph 构建 |
| DocumentPage | 文档上传、RAG 搜索、问答 |
| TablePage | 表格结构识别 |
| WikiPage | LLM Wiki 搜索 |
| SkillPage | Skill 管理、匹配、执行、文件上传、下载 |
| EvaluationPage | 评测仪表盘 |
| ModelConfigPage | 模型参数配置 |

**api/index.ts** — 34 个 API 函数，axios 封装

> **修复**：Skill 管理"运行"按钮无响应（添加 execModal 弹窗）
> **修复**：执行结果无下载链接（添加可点击下载按钮）
> **修复**：生成内容为占位符（改为调用 AI 自动生成章节内容）
> **修复**：表格结构化数据无显示（data_rows 字段）

### 步骤 7：测试与评测

**SWE-bench Lite 数据集下载**：
```bash
pip install datasets
python test_data/coding/_download_swe_bench.py
```
包含 300 个实例，来自 12 个开源仓库。

**批量测试脚本**：batch_test_swe_bench.py
- 逐一调用 Coding Agent API
- 统计任务完成率、平均轮数、耗时、缓存指标
- 按仓库细分统计
- 结果保存为 JSON



### 步骤 8：文档与演示

- **AGENTS.md** — Agent 系统配置文档
- **AI桌面端系统操作手册.md** — 完整操作指南（14 章节）
- **AI桌面端系统汇报.pptx** — 14 页汇报 PPT
- **项目搭建过程.md** — 本文件

---

## 三、环境配置

### 模型配置

| 参数 | 值 |
|------|-----|
| Provider | deepseek |
| API Base | https://api.deepseek.com/v1 |
| Model | deepseek-chat |
| API Key | sk-7ae690757ac74f8a868b1fa42c3106d7 |
| Max Tokens | 393,216（DeepSeek 上限） |
| Context Length | 1,000,000 |

### 文档生成环境

Markdown 报告直接生成 .md 文件，无需外部编译环境。

---

## 四、已修复问题清单（34 项）

| # | 问题 | 修复位置 | 修复方式 |
|---|------|---------|---------|
| 1 | max_tokens=1M 超出 DeepSeek 限制 | model_config.json | 改为 393,216 |
| 2 | JSON 文件 BOM 导致解析失败 | config.py | utf-8-sig 读取 |
| 3 | 表格 data 字段格式不匹配 | table_recognizer.py + TablePage.tsx | 新增 data_rows |
| 4 | sentence_transformers 异常 | vector_store.py | except Exception |
| 5 | defaultdict 无 most_common() | llm_wiki.py | 改为 Counter |
| 6 | Word 中文路径保存失败 | word_skill.py | re.sub 过滤字符 |
| 9 | ~~LaTeX 编译~~ 已移除 | latex_skill.py 已删除 | 改用 Markdown 报告 |
| 10 | ~~LaTeX 编译~~ 已移除 | latex_skill.py 已删除 | 改用 Markdown 报告 |
| 11 | ~~LaTeX 模板~~ 已移除 | latex_skill.py 已删除 | 改用 Markdown 报告 |
| 12 | Agent tool_calls 格式错误 | agent/__init__.py + reasonix.py | content 设为 None |
| 13 | 表格 CSV 作为文件路径 | main.py | 逗号→Tab 后文本解析 |
| 14 | Skill 运行按钮无反应 | SkillPage.tsx | 添加 execModal |
| 15 | 生成文件无下载链接 | SkillPage.tsx | 添加下载按钮 |
| 17 | Word/Markdown 内容为占位符 | SkillPage.tsx | AI 自动生成内容 |
| 18 | uvicorn 热重载冲突 | run.py | reload=False |
| 19 | Checkpoint 跨任务丢失 | agent/__init__.py + reasonix.py | task_state → self.checkpoints |
| 20 | 回滚无法多级 | main.py | 仅删除被回滚的 checkpoint |
| 21 | 回滚不删新建文件 | agent/__init__.py | _save_initial_files + is_new_file |
| 22 | Ollama 慢导致超时 | api/index.ts + agent | 超时 120s → 600s / 300s |
| 23 | Cache 重置路由 404 | main.py | 迁移端口后注册 reset 路由 |
| 24 | 查看详情不显示文件 | AgentPage.tsx | 改用 edited_files 字段 |
| 25 | 模型配置加载卡死 | main.py | socket → asyncio.open_connection |
| 26 | Vite 代理端口不匹配 | vite.config.ts | 8000 → 18327 |
| 27 | 启动器闪退 | 启动桌面端.bat | 修复引号/编码/路径，加超时 |
| 28 | Ollama 本地连接不可达 | main.py | localhost IPv6 失败回退 127.0.0.1 |
| 29 | Skill 匹配不生效 | skill_manager.py | 技能名+格式关键词评分 |
| 30 | 匹配后全部技能执行 | SkillPage.tsx | executing → executingSkill 单技能标记 |
| 31 | doc-ppt-offline 无输出 | doc_ppt_offline_skill.py | 运行完整原生流水线产出 PPTX |
| 32 | excel-ppt 无完整 PPT | excel_ppt_skill.py | 运行完整 7 步流水线 |
| 33 | Skill 列表布局重叠 | SkillPage.tsx | 标签换行、flex-start 对齐 |
| 34 | 执行历史无法清空 | main.py + SkillPage.tsx | 新增 /skills/history/clear |

## 五、最新功能

### 代码版本 / Diff 查看

- **后端**：`CodingAgent.file_versions` 记录每个文件的版本链（original/new/modified），`get_file_diff` 用 Python `difflib` 生成任意两版本间 unified diff
- **API**：`GET /api/agent/file-versions`、`GET /api/agent/file-content`、`GET /api/agent/file-diff`
- **前端**：Coding Agent 右侧"代码版本 / Diff"卡片，点击版本查看该版本代码与 Diff（红色=删除，绿色=新增），回滚后自动刷新

### 对话历史记录（会话独立回滚）

- 每个对话作为独立会话保存消息、Checkpoint、文件版本
- 会话可切换查看，回滚互不影响，持久化到 `outputs/conversation_history.json`
- 会话标题自动取首条用户消息
- **API**：`GET/POST/DELETE /api/agent/sessions`、`POST /api/agent/sessions/{id}/switch`

### 页面状态保持（Keep-Alive）

- 切换功能页时原页面状态保留（`display: none` 隐藏而非卸载）
- 各页补充"刷新"按钮手动更新数据

### Skill 系统规范优化（对齐 NVIDIA Verified Coding Agent Skills）

- 为技能创建标准 SKILL.md 文件 + Skill Card 治理元数据
- 签名校验与最小权限约束，未验证第三方技能标记不可信
- 5 个内置技能：markdown-report、word-lab-report、doc-ppt-online、doc-ppt-offline、excel-ppt
- markdown 轻量 GFM 技术笔记 / word 正式编号报告（1/1.1/1.1.1、图X表X、GB/T 7714）
- doc-ppt-offline 与 excel-ppt 运行完整原生流水线产出 .pptx

### 输出目录统一

- `output_dir` 自动指向项目根 `outputs/`（config.py 自动检测项目根目录）
- Skill 产物、对话历史、表格导出、PPT 生成全部输出到项目根 `outputs/`

### 桌面客户端（Electron）

- `desktop/main.js`：原生窗口（1400×900）+ 系统托盘 + 后端子进程管理
- `npm run build` + `npm run dist` 打包为 exe
- 前端/后端资源内置于 `resources/` 目录
- 后端端口统一为 **18327**

---

## 六、运行方式

```bash
# 方式一：双击 启动桌面端.bat（Web 版）
# 后端端口 18327，自动打开浏览器 http://localhost:3000

# 方式二：命令行
# 启动后端（端口 18327）
cd backend
pip install -r requirements.txt
set API_PORT=18327
python run.py

# 启动前端（端口 3000，Vite 代理到 18327）
cd frontend
npm install
npm run dev

# 访问
# 前端: http://localhost:3000
# 后端 API: http://localhost:18327
# API 文档: http://localhost:18327/docs

# 方式三：桌面客户端
# desktop/dist/win-unpacked/AI桌面端系统.exe（原生窗口，双击运行）
```
